
EBIN = .\ebin\ 

DEL = del /Q



all: compile


compile:
	for %%1 in ($(ARMY_DIRS)) do if not exist $(EBIN)%%1 mkdir $(EBIN)%%1
	erl -make


clean:
	$(DEL) $(EBIN)
	for %%1 in ($(ARMY_DIRS)) do $(DEL) $(EBIN)%%1


test: clean compile
	erl -nologo -noshell -pz ebin -s testAll test -s init stop