-module(ui)

gettime() ->
	% get system time, return int
	todo

getchess(x,y) ->
	% get chess at x, y, return chess
	todo
getchessById(Id, Army) ->
	% get chess at x, y, return chess
	todo
getchessAll() ->
	% get all chess living
	todo	

sendCommand(Command) ->
	% send command		
	todo
